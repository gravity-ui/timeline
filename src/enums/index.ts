export enum ComponentType {
  Axes = "Axes",
  Grid = "grid",
  Ruler = "ruler",
  Events = "events",
  Markers = "markers",
}

export enum StrokeMode {
  STRAIGHT,
  DASHED,
}

export enum TimelineState {
  INIT,
  READY,
}
